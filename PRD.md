# 产品需求文档

## 1. 项目概览

水土保持行业报告编制工具箱，面向咨询报告编制者。技术栈：Next.js 16 + React 19 + Tailwind CSS 4 + Tauri 2。

开发纪律：每次开发前打开本文档 → 找到要做的功能 → 看验收标准 → 开发 → 做完更新状态。

## 1.1 发布记录

### v0.1.0（2026-08-24）— 首个桌面版

- 措施体系图：导入 Excel 体系表，生成 Draw.io 框图
- 进度图：甘特图展示，月/季度切换，导出 PNG/Excel，模板保存/加载
- 水力计算：多沟渠管理，断面/砌体参数输入，暴雨强度和洪峰流量参数，导出 Excel
- 工作台首页：核心三模块入口卡片
- Tauri 2 桌面打包（Windows x64）
- 侧边栏仅展示核心模块，降级模块隐藏（保留路由可直接访问）

### v0.2.0（2026-08-28）— 进度图智能化 + 水力计算逻辑完成

- 进度图：措施体系图一键导入 + 自动排期（可配置工期/顺序）+ 智能合并 + 甘特图拖拽调整 + 条目持久化
- 水力计算：计算逻辑全部接入（曼宁谢才、流速流量、过流验算、断面几何、砌体工程量、汇流历时、Cp/Ct/m1 查表），模块基本可用
- 流失量模块立项：依据 SL 773-2018《生产建设项目土壤流失量测算导则》开始需求设计

## 2. 功能需求

### 2.1 项目建设特性表 `/project-profile`【降级 — 暂不使用】

- [x] UI 完成

管理项目建设基础数据。各模块独立运作，不依赖此模块作为中央数据源。从侧边栏隐藏，保留路由可直接访问。

- 支持创建多个特性表并命名、可重命名
- 表结构：防治分区 | 占地面积 | 土石方开挖 | 土石方回填 | 项目自身利用方 | 综合利用方 | 弃方
- 平衡校验：土石方开挖 - 土石方回填 = 项目自身利用方 + 综合利用方 + 弃方
- 可扩展列定义系统，新增字段只需追加列定义
- 数据持久化至 localStorage

**验收标准：**
- [x] 可创建、重命名、删除特性表
- [x] 表格列与上述结构一致
- [x] 平衡校验公式正确，校验不通过时有提示
- [x] 新增列定义只需修改一处配置
- [x] 刷新页面后数据不丢失

---

### 2.2 进度图 `/progress`

- [x] UI 完成
- [x] 措施体系图导入 + 自动排期

项目施工进度甘特图。

- 支持月/季度切换
- 支持导出 PNG 和 Excel
- 支持保存/加载模板
- 从措施体系图一键导入措施项，自动排期，甘特图上拖拽微调

**Phase 1 验收标准：**
- [x] 可添加、编辑、删除进度条目
- [x] 月/季度视图切换正常
- [x] PNG 导出图片清晰、内容完整
- [x] Excel 导出数据与界面一致
- [x] 模板保存后可正确加载

**Phase 1.5 验收标准（导入 + 自动排期）：**
- [x] 一键从措施体系图导入所有措施项（`progressImport.ts`）
- [x] 分区映射为完整路径："一级分区-二级分区-三级分区"
- [x] 措施类型字段：工程措施 / 植物措施 / 临时措施
- [x] 自动排期按措施类型排序：工程措施 → 临时措施 → 植物措施
- [x] 同类型内按措施体系图中的出现顺序排列
- [x] 默认工期可配置，初始值：工程措施 6 月 / 临时措施 3 月 / 植物措施 4 月
- [x] 施工顺序规则可配置
- [x] 导入时智能合并：同名措施项保留已有时序，只新增不存在的
- [x] 措施体系图更新后，手动再点导入即可同步新增项
- [x] 甘特图上支持拖拽调整开始/结束时间（move/resize，5 天步距）
- [x] 清空示例数据，进度图从措施体系图获取真实数据

**技术债清理：**
- [x] 年份下拉改为动态生成（基于实际数据范围），去掉硬编码 [2024-2028]
- [x] 进度条目持久化至 localStorage（`swc-gantt-items`，刷新不丢失）
- [ ] GanttChart 和 GanttPrintView 共享渲染逻辑，消除代码重复
- [ ] genId / generateId 去重（`GanttTemplatePanel.tsx` 仍有独立 `genId`）

---

### 2.3 水力计算 `/hydraulic-calc`

- [x] UI 完成
- [x] 计算逻辑接入

依据 GB 51018-2014 附录A.4 截排水设计规范，进行排水沟水力计算。

**UI 验收标准（Phase 1）：**
- [x] 多沟渠管理：左侧列表，支持新建、重命名、删除、切换
- [x] 三种断面形式可选：矩形、梯形、抛物线形
- [x] 五种砌体类型可选：砖砌、浆砌石、混凝土、土质、种草
- [x] 断面参数输入：底宽B、深度H、边坡比m
- [x] 砌体参数输入：壁厚、底板厚
- [x] 暴雨强度参数输入：q = Cp × Ct × q5,10
- [x] 设计洪峰流量参数输入：Qm = 16.67 × φ × q × F，支持 km²/hm²
- [x] 地表类型选择：16 种，自动填入径流系数参考值
- [x] 安全加高：按工程等级（1级0.3m，2/3级0.2m）
- [x] 回填经验系数：1.00~1.20 可选
- [x] 总览表格显示所有沟渠核心参数
- [x] 导出 Excel：计算参数表 + 单位工程量表
- [x] 数据持久化至 localStorage

**Phase 2 验收标准（计算逻辑）：**
- [x] 谢才系数按曼宁公式正确计算（`chezyCoefficient`，C = (1/n)·R^(1/6)）
- [x] 流速、流量计算结果与手算一致（公式正确；手算对照验证待补）
- [x] 过流验算：设计流量 ≤ 计算流量时通过（Q ≥ Qm，UI 红绿提示 + Excel 导出列）
- [x] 各断面形式的几何参数计算正确（面积、湿周、水力半径，矩形/梯形/抛物线）
- [x] 砌体工程量计算正确（砌体体积、抹面面积、土方开挖量；⚠️ 外深 `outerH = H+t+tb` 的建模假设与典型手算 H+tb 可能不一致，待与手算样本核对）

**已知待办（小修，不阻塞使用）：**
- [ ] 谢才系数 C 已计算但结果表与 Excel 导出未展示（流程.md §9.2 要求输出）
- [ ] `docs/hydraulic-calc-spec.md` 为旧版方案（τ=0.278L/v 等），与代码遵循的 `docs/水力计算流程.md` 冲突，需废弃或合并
- [ ] `docs/水力计算流程.md` §4.2 的 t2 仍写"预留后续细化"，实际已实现，文档待同步

---

### 2.4 流失量计算 `/erision-calc`【当前 — 立项开发中】

- [x] Phase 1 第 1 条：Excel 模板下载 + 导入（三级预测单元 + 占地类型 + 面积 + 参数列）

依据 SL 773-2018《生产建设项目土壤流失量测算导则》（水利部公告 2018 年第 9 号，2018-10-23 发布 / 2019-01-23 实施），配套 GB 50433-2018《生产建设项目水土保持技术标准》，进行生产建设项目土壤流失量测算。用于水土保持方案编制阶段的流失量预测。

**第一版范围：**
- 只做**坡面水蚀**（一般扰动地表、工程开挖面、工程堆积体及其三级分类），不做风蚀、沟蚀、重力侵蚀、冻融侵蚀
- 项目范围以湖南为主，附录 C 分县 R/K 表只入库湖南省分县
- 公式体系：M = R·K·L_y·S_y·B·E·T·A（一般扰动地表，翻扰型换 K_yd，新增流失量 ΔM 按扰动前后 B·E 变化）；M = G·R·L·S·A / M = F·G·L·S·A + M_w（开挖面）；M_dw = X·R·G_dw·L_dw·S_dw·A（堆积体）；坡长因子 L = (λ/20)^0.5，λ>100m 取 100m

**数据模型：**
- 三级预测单元：一级=防治分区，二级=扰动二级分类（一般扰动地表 / 工程开挖面 / 工程堆积体），三级=扰动三级分类（植被破坏型 / 地表翻扰型 / 上方无来水 / 上方有来水）
- 占地类型按 GB/T 21010 土地利用现状分类大类（耕地/林地/草地/园地/水域及水利设施用地/其他土地…）
- **项目为顶层实体**（一个项目 = 单元/时段/结果，localStorage 持久化），为将来"一站式编辑器/项目级数据互通"预留结构

**输入（Excel 模板）：**
- 软件提供标准 Excel 模板下载：行=三级预测单元（多列递进），列=面积(hm²)｜施工期面积｜自然恢复期面积｜坡度θ｜投影坡长λ｜植被覆盖度(%)｜R｜K
- 导入后软件侧：按扰动类型自动匹配公式 → 选湖南县自动带出 R/K → 计算 LS/C/P/G/S 等因子 → 输出流失量

**输出（一个 Excel 5 个 sheet）：**
1. 原地貌土壤侵蚀模数取值表（按占地类型）
2. 扰动地表及损坏植被面积统计表（由占地类型推导，如林地、草地损毁面积）
3. 流失预测单元及预测时段一览表（施工期面积/自然恢复期面积/施工期预测时间/恢复期时间；后期可联动进度图）
4. 扰动类型侵蚀模数计算表（因子明细 → 流失量）
5. 水土流失预测总表（分时段汇总，含合计行）

**时段：** 预测面积分列（施工期 + 自然恢复期分别计算），施工期默认 1 年、恢复期默认 2 年，可配置。

**交互要求（沿用现有模块习惯）：**
- 数值步距：坡度 0.5°、面积 0.01 hm²、坡长 1m、覆盖度 1%，支持键盘上下键/滚轮微调
- 查表参数（R/K）选县下拉自动带出，手动覆盖显示"自定义"标记
- 选好扰动三级分类后 B/E/T/G 等因子自动带默认值，用户只改差异项
- 类 Excel 编辑：双击编辑、Tab/Enter 移动、支持整块复制粘贴多行
- 导入校验即时反馈：问题行高亮（面积缺失/覆盖度超 100%/坡长>100），点行定位
- 行内实时预览流失量，异常值弱提示
- 导出前提示未完成单元数（可跳过）

**参考文档：**
- 调研报告：`docs/sl773-土壤流失量测算规范.md`
- 标准原文扫描件：`docs/reference/SL 773-2018 生产建设项目土壤流失量测算导则_湖南地区.pdf`

**验收标准（Phase 1）：**
- [x] Excel 模板可下载、可导入（三级预测单元 + 占地类型 + 面积 + 参数列）
- [ ] 湖南分县 R/K 查表（附录 C 数据入库，选县自动带出）
- [ ] 坡面水蚀公式 + 新增流失量 ΔM 计算正确（与扫描件算例核对）
- [ ] 5 张表导出（一个 Excel 5 sheet）
- [ ] 项目实体建模 + localStorage 持久化
- [ ] 页面 UI（复用 `/erision-calc` 路由重新设计）

---

### 2.5 指标对比分析 `/indicator-compare`【降级 — 暂不使用】

- [x] UI 完成

水土保持指标变更对比分析。从侧边栏隐藏，保留路由可直接访问。

- 支持 53 号令和 14 号文两套标准切换
- 表头：条款 | 条款说明 | 原水土保持方案 | 变更方案 | 变化情况 | 是否涉及一般变更 | 是否涉及重大变更
- 原方案和变更方案手动输入
- 是否涉及变更由系统自动判断（阈值规则 + 关键词匹配）
- 同条款自动合并显示

**验收标准：**
- [ ] 两套标准可切换，表头和条款随之变化
- [ ] 手动输入原方案和变更方案后，变化情况自动计算
- [ ] 一般变更/重大变更判断逻辑正确
- [ ] 同条款自动合并显示

---

### 2.6 措施体系图 `/measure-system`

- [x] UI 完成
- [x] 核心逻辑完成

导入 Excel 措施体系表，自动生成 Draw.io 措施体系框图。同时作为**进度图**的数据源。

- 支持列：一级分区、二级分区、三级分区、工程措施、植物措施、临时措施
- 自动生成 Draw.io XML 格式框图
- 层级布局：一级分区 → 二级分区 → 三级分区 → 措施容器（工程/植物/临时三列）
- 支持保存/加载模板
- 下载 .drawio 文件后用 Draw.io 打开编辑、导出 PNG

**验收标准：**
- [x] Excel 导入正确解析 6 列数据
- [x] 生成的 .drawio 文件可在 Draw.io 中正常打开
- [ ] 层级布局和连线正确
- [ ] 模板保存/加载正常
- [x] 数据持久化至 localStorage

---

### 2.7 设置 `/settings`

- [ ] 待开发

**验收标准：**
- [ ] 待定

---

### 2.8 工作台首页 `/`

- [x] UI 完成

工具入口卡片 + 最近使用列表，快速跳转各功能模块。

**验收标准：**
- [x] 各功能模块入口可点击跳转
- [x] 最近使用列表正常显示

## 3. 开发阶段

### Phase 1 — 核心三模块 UI 壳【已完成】

- [x] 现代简洁风（浅色侧边栏 + 卡片式布局）
- [x] 工作台首页（工具入口 + 最近使用）
- [x] 措施体系图（UI + 核心逻辑）
- [x] 进度图（UI）
- [x] 水力计算（UI）
- [x] 项目建设特性表（UI，已降级）
- [x] 指标对比分析（UI，已降级）

### Phase 1.5 — 进度图智能化【已完成（遗留 2 项技术债）】

- [x] 措施体系图数据一键导入进度图
- [x] 按措施类型自动排期（可配置默认工期和顺序）
- [x] 导入时智能合并（同名保留，新增自动排期）
- [x] 甘特图拖拽调整时序
- [x] 进度条目 localStorage 持久化
- [x] 年份下拉动态化
- [ ] GanttChart 和 GanttPrintView 渲染逻辑去重
- [ ] genId / generateId 去重

### Phase 2 — 水力计算逻辑接入【已完成（3 项小修待办）】

- [x] 谢才系数按曼宁公式正确计算
- [x] 流速、流量计算结果与手算一致（手算对照验证待补）
- [x] 过流验算：设计流量 ≤ 计算流量时通过
- [x] 各断面形式几何参数计算正确（面积、湿周、水力半径）
- [x] 砌体工程量计算正确（外深 outerH 建模假设待与手算样本核对）

**待办小修：**
- [ ] 谢才系数 C 展示到结果表与 Excel 导出
- [ ] 废弃旧版 `docs/hydraulic-calc-spec.md` 或与 `docs/水力计算流程.md` 合并
- [ ] `docs/水力计算流程.md` §4.2 t2 文档同步

### Phase 3 — 流失量计算（SL 773-2018）【当前】

- [ ] 湖南分县 R/K 附录 C 数据入库
- [ ] 坡面水蚀公式库 + ΔM（6 子类）
- [ ] Excel 模板下载/导入 + 项目实体建模 + localStorage 持久化
- [ ] 5 张表导出
- [ ] 页面 UI 重新设计（复用 `/erision-calc` 路由）
- [ ] 公式与扫描件算例核对验证

### Phase 4 — 后端对接【远期】

- [ ] Tauri 后端 Rust 服务
- [ ] 文件系统持久化（替代 localStorage）
- [ ] 项目级数据管理（与流失量模块"项目实体"结构衔接）
- [ ] Excel 导入导出增强
- [ ] Draw.io 渲染引擎集成（页面内预览）

### Phase 5 — 一站式编辑器【远期构想】

- [ ] 多模块数据互通（措施体系图分区 → 流失量预测单元 → 进度图排期）
- [ ] 统一项目工作台（预留，不阻塞当前开发）
